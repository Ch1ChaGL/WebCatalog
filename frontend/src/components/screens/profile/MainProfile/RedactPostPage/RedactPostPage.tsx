import React from 'react';
import styles from './RedactPostPage.module.css';

const RedactPostPage = ({ postId }: { postId: string }) => {
  return <div>RedactPostPage</div>;
};

export default RedactPostPage;
